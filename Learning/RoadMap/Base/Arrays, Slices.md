```Go
// Объявление
slice := []int{1,2,3} // Неограниченное кол-во
array := [3]int{1,2,3} // Максимум 3 айтема
```

```Go
length := len(slice) // Длина слайса
capacity := cap(slice) // Вместимость слайса

// Добавление элементов в слайс
slice := []int{1, 2, 3}
slice = append(slice, 4,3,5,2,11,7) // Добавление новых айтемов в слайс
slice = append(slice, slice2) // Добавление слайса в слайс
```

```Go
// Удаленрие по индексу из слайса
slice := []int{1, 2, 3, 4, 5}
index := 2 // Удаляем элемент с индексом 2
slice = append(slice[:index], slice[index+1:]...) // [1, 2, 4, 5]

// Удаление последнего элемента
slice = slice[:len(slice)-1] // [1, 2, 4]

// Удаление первого элемента
slice = slice[1:] // [2, 4]
```